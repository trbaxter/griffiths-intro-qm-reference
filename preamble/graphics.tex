% ===================================================== %
%  Settings & packages related to graphics generation.
% ===================================================== %


%% Allows direct inclusion of images & image transformation capabilities
\RequirePackage{graphicx}


%% Allows drawing vector graphics, diagrams, annotated figures, and custom visual structures
\RequirePackage{tikz}
\usetikzlibrary{
    calc,
    decorations,
    decorations.pathreplacing,
    decorations.markings,
    decorations.text,
    positioning,
    scopes}


%%
\RequirePackage{float}


%%
\RequirePackage[skip=1.5ex]{caption}


%%
\RequirePackage{subcaption}
\captionsetup{labelsep=period}