% ================================================= %
%       Settings & packages related to lists.
% ================================================= %

% Supports tight spacing, custom labels, and indentation control

\RequirePackage[inline]{enumitem}

%

\RequirePackage[parfill]{parskip}