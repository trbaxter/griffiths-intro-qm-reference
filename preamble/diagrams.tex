%% Enhances & customizes behavior of figure/table captions
\RequirePackage{caption, subcaption}

%% Enhances LaTeX's handling of floating environments (figure/table) by adding [H]
\RequirePackage{float}