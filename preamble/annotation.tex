% ============================================================= %
%  Settings & packages related to text markings & decorations.
% ============================================================= %


% Provides underline, strikethrough, cross-out text decorations.
\RequirePackage[normalem]{ulem}


% Spaced-Out-Underlining-Layout; complements ulem with overlapping features.
\RequirePackage{soul}